name: Pages — viewer static

# Build viewer (React, mode data statis) + salin dataset JSON ke hasil build,
# lalu deploy ke GitHub Pages. Tanpa server, tanpa API key.
on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - 'dataset/**'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install
        working-directory: frontend
        run: npm ci

      - name: Build (mode data statis)
        working-directory: frontend
        env:
          VITE_DATA_MODE: static
        run: npm run build -- --base=/${{ github.event.repository.name }}/

      - name: Salin dataset ke hasil build
        run: |
          mkdir -p frontend/dist/dataset
          cp -r dataset/. frontend/dist/dataset/

      - uses: actions/configure-pages@v5
        with:
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
